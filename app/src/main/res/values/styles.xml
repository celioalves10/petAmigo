<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!--style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar"-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

    <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ReisedButtom" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint" tools:targetApi="lollipop">@color/colorPrimaryDark
        </item>
        <item name="android:animation">@android:anim/fade_out</item>
    </style>

    <style name="flaButtom" parent="Widget.AppCompat.Button.Borderless">
        <item name="backgroundTint" tools:targetApi="lollipop">@color/colorPrimaryDark
        </item>
        <item name="android:animation">@android:anim/fade_out</item>
        <item name="itemPadding">9dp</item>
    </style>

    <style name="AppTheme.NoActionBar.navheader">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor" tools:targetApi="lollipop">@android:color/transparent
        </item>
    </style>

    <style name="FacebookLoginButton">
        <item name="android:textSize">15sp</item>
        <item name="textAllCaps">false</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@color/backgroundcolor</item>
        <item name="android:paddingLeft">13dp</item>
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="android:paddingTop">11dp</item>
        <item name="android:paddingBottom">11dp</item>
        <item name="android:layout_marginLeft">4dp</item>
        <item name="android:layout_marginRight">5dp</item>
    </style>

    <style name="Theme.Transparent" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="SCBSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/colorPrimary</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/colorPrimary</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/colorPrimaryDark</item>
    </style>
</resources>

